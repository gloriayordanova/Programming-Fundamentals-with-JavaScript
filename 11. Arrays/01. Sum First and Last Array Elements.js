function solve(nums){
    console.log(nums[0] + nums[nums.length - 1]);
}

solve([20, 30, 40]);
solve([10, 17, 22, 33]);
solve([11, 58, 69]);
//solve([5]); in that example will return 10 instead of 5, so be aware of this