function solve(num) {
    console.log(num * 2);
}

solve(2);
solve(5);
solve(20);




// old example from basics, not used anymore
function multiply(input){
    let NumAsString = input[0];
    let num = Number(NumAsString);
    console.log(num * 2);
}

multiply(['2']);
multiply(['5']);
multiply(['20']);